# This project is my implementation of the Greg Lim book Beginning Node.js, Express and MongoDB development.

## To initialise

Run `npm install`.

## To run

Run `npm start` and navigate to `localhost:4000` in a web browser.
